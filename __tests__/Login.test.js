import { Login } from "../app/(main)/profile/login";

describe("login screen", () => {
  it("should go to Workouts Main on login", () => {
    const page = render(<Login />);
    expect(false).toBeTruthy();
  });
});
